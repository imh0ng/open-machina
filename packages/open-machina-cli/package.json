{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "open-machina-cli",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "open-machina-plugin": "workspace:*",
    "open-machina-shared": "workspace:*"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  }
}
